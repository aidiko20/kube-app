apiVersion: apps/v1
kind: Deployment
metadata:
  name: vproapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vproapp
  template:
    metadata:
      labels:
        app: vproapp
    spec:
      containers:
        - name: vproapp
          image: aidiko20/vprofileapp:V1
          ports:
            - name: vproapp-port
              containerPort: 8080
      initContainers:
        - name: init-mydb
          image: busybox
          command: ['sh', '-c', 'untill nslookup vprodb; do echo waiting for mydb; sleep 20; done']
        - name: init-memcache
          image: busybox
          command: ['sh', '-c', 'untill nslookup vprocache01; do echo waiting for memcache; sleep 20; done']          
